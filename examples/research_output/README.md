# Compact Research Output Example

This fictional output demonstrates the shape of the Obsidian vault without
making real protocol claims. It is intentionally small; a real project creates
the full topic set only when relevant documentation is available.

Open this folder in a Markdown editor or copy its contents into a temporary
Obsidian vault to inspect the links. Generated project output normally lives
under `output/vault/` and is ignored by Git.
